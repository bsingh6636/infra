# Service-specific non-secret values for subsnepal-api.
# REDIS_KEY_PREFIX namespaces this service's Redis keys so they can't collide
# with another service's keys on the shared redis container.
REDIS_KEY_PREFIX=subsnepal
