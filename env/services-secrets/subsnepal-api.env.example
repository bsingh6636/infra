# Service-specific secret values for subsnepal-api.
# Connects to this repo's own redis service (config/stack.yaml) over the
# shared compose network, not the external VERCE_REDIS_URL fallback.
REDIS_URL=redis://:<REDIS_PASSWORD from env/services-secrets/redis.env>@redis:6379
